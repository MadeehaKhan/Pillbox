export class MedTrigger{
    id: number; //to match the medication ID
    name: string;
    medInfo: string;
    every: string;
    count: number;
    refills: number;
    hour: number;
    minute: number;
}